# Simple Version Control

# Installation steps:
sudo python setup.py install

# Usage:
svc filename   ; To commit a file
svc N          ; Output Nth version of the file.

# Operations:
- Delete a line from the file
- Append a new line anywhere in the file
